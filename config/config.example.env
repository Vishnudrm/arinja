# Arinja Configuration Example
# -------------------------
# 1. Copy this file to config/config.env: 
#    cp config/config.example.env config/config.env
#
# 2. Update the values with your actual configuration
# 3. Keep config.env secure and never commit it to the repository

# PostgreSQL Database Configuration
# -------------------------------
# Format: postgresql://username:password@host:port/database
# Example for local development:
#   POSTGRES_URI=postgresql://user:password@localhost:5432/arinja
# Example for production:
#   POSTGRES_URI=postgresql://user:strong_password@db.example.com:5432/arinja_prod

POSTGRES_URI=postgresql://user:password@localhost:5432/arinja

# Security Notice
# -------------
# 1. Never commit the actual config.env file to version control
# 2. Use strong passwords in production
# 3. Consider using environment-specific configurations
